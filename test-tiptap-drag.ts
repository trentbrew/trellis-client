import { TextSelection, NodeSelection } from "prosemirror-state"

// How to select a node in prosemirror:
// const selection = NodeSelection.create(doc, pos)
// tr.setSelection(selection)
// view.dispatch(tr)
